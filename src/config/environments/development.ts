import type { AdminConfig } from './types';

export const developmentConfig: AdminConfig = {
  env: 'development',
  apiUrl: 'http://localhost:8000/api/v1',
  appName: 'RESVEPRO',
  appTagline: 'Restaurando Verdades Proféticas',
};
